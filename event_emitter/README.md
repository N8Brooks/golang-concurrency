# Event Emitter

This package models the LeetCode `Event Emitter` problem: subscribe callbacks
by event name, emit them in subscription order, and allow unsubscribe.

## Testing

Run the challenge tests:

```sh
go test -tags challenge ./event_emitter
```

Run the hint solutions:

```sh
go test ./event_emitter/solutions
```

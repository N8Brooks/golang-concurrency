//go:build challenge

// Package throttle contains the challenge version of the Throttle problem.
package throttle

import "time"

func Throttle(fn func(...int), d time.Duration) func(...int) {
	panic("unimplemented")
}

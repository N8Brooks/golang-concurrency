# Timeout Cancellation

This package models the LeetCode `Timeout Cancellation` problem: schedule one
future call and return a cancel function that can prevent it from running.

## Testing

Run the challenge tests:

```sh
go test -tags challenge ./timeout_cancellation
```

Run the hint solutions:

```sh
go test ./timeout_cancellation/solutions
```

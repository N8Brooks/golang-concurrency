//go:build challenge

// Package debounce contains the challenge version of the debounce problem.
package debounce

import "time"

func Debounce(fn func(...int), t time.Duration) func(...int) {
	panic("unimplemented")
}

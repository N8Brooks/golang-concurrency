# Custom Interval

This package models the LeetCode `Custom Interval` problem: create an interval
with growing waits and provide a way to cancel it.

## Testing

Run the challenge tests:

```sh
go test -tags challenge ./custom_interval
```

Run the hint solutions:

```sh
go test ./custom_interval/solutions
```

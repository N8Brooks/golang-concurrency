# Interval Cancellation

This package models the LeetCode `Interval Cancellation` problem: execute
immediately, repeat at a fixed interval, and stop when canceled.

## Testing

Run the challenge tests:

```sh
go test -tags challenge ./interval_cancellation
```

Run the hint solutions:

```sh
go test ./interval_cancellation/solutions
```

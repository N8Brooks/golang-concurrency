//go:build challenge

// Package interval_cancellation contains the challenge version of the Interval
// Cancellation problem.
package interval_cancellation

import "time"

func Cancellable(fn func(...int), args []int, t time.Duration) func() {
	panic("unimplemented")
}

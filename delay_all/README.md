# Delay All

This package models the LeetCode `Delay the Resolution of Each Promise`
problem.

## Testing

Run the challenge tests:

```sh
go test -tags challenge ./delay_all
```

Run the hint solutions:

```sh
go test ./delay_all/solutions
```

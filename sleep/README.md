# Sleep

This package models the LeetCode `Sleep` problem: return a promise-like value
that resolves after a given delay.

## Testing

Run the challenge tests:

```sh
go test -tags challenge ./sleep
```

Run the hint solutions:

```sh
go test ./sleep/solutions
```
